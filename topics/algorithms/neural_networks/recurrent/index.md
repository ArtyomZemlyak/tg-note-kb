# Recurrent Networks

This section contains information about recurrent neural networks (RNNs).

## Contents

- [[memory_caching_rnns.md]] — Memory Caching: фреймворк для RNN с растущей памятью, кэширующий чекпоинты состояний памяти для сегментов последовательности

## Overview

Recurrent Neural Networks (RNNs) are a class of artificial neural networks where connections between nodes form a directed graph along a temporal sequence. This allows it to exhibit temporal dynamic behavior for a time sequence. Unlike feedforward neural networks, RNNs can use their internal state (memory) to process sequences of inputs.

## Современные архитектуры

Современные RNN включают различные архитектуры, такие как линейное внимание (Linear Attention), structured state-space модели (Mamba, SSM), и гибридные подходы, сочетающие эффективность RNN с ёмкостью трансформеров.

```metadata
category: machine_learning
subcategory: neural_networks
tags: ml, neural_networks, rnn, lstm, gru, sequential_data, deep_learning, algorithms
```