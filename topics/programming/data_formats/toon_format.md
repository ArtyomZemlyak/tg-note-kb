# Формат TOON (Token-Oriented Object Notation)

## Описание

**Token-Oriented Object Notation (TOON)** - это компактный, удобочитаемый формат сериализации данных, оптимизированный для использования в контексте Large Language Models (LLM), обеспечивающий 30-60% сокращение количества токенов по сравнению с JSON для табличных данных. Формат сочетает в себе структуру на основе отступов, как в YAML, и табличный формат, как в CSV, чтобы оптимизировать использование токенов в LLM.

## Основные особенности

- 💸 **Экономия токенов:** обычно на 30-60% меньше токенов, чем JSON
- 🤿 **Надежные защиты для LLM:** явные длины и поля позволяют проводить валидацию
- 🍱 **Минимальный синтаксис:** удаляет избыточную пунктуацию (фигурные скобки, квадратные скобки, большинство кавычек)
- 📐 **Структура на основе отступов:** как YAML, использует пробелы вместо фигурных скобок
- 🧺 **Табличные массивы:** объявляет ключи один раз, передает данные в виде строк

## Пример сравнения JSON и TOON

### JSON (подробный формат)
```json
{
  "users": [
    { "id": 1, "name": "Alice", "role": "admin" },
    { "id": 2, "name": "Bob", "role": "user" }
  ]
}
```

### TOON (компактный формат)
```
users[2]{id,name,role}:
  1,Alice,admin
  2,Bob,user
```

## Синтаксис

### Объекты

Простые объекты с примитивными значениями:
```
id: 123
name: Ada
active: true
```

Вложенные объекты:
```
user:
  id: 123
  name: Ada
```

### Массивы

#### Примитивные массивы (встроенные)
```
tags[3]: admin,ops,dev
```

#### Массивы объектов (табличный формат)

Когда все объекты имеют одинаковые поля и примитивные значения, TOON использует эффективный **табличный формат**:
```
items[2]{sku,qty,price}:
  A1,2,9.99
  B2,1,14.5
```

## Технические характеристики

- **Версия спецификации:** 1.3
- **Тип данных:** строка | число | логическое значение | null | объект | массив
- **Индентация:** по умолчанию 2 пробела на уровень
- **Разделители:** запятая (по умолчанию), табуляция или символ | (pipe)
- **Кодировка:** UTF-8 с окончаниями строк в формате LF

## Использование в LLM

TOON особенно полезен при передаче структурированных данных LLM, где количество токенов имеет значение. Формат наиболее эффективен для:
- Единообразных массивов объектов (одинаковые поля, примитивные значения)
- Особенно для больших наборов данных с последовательной структурой

## Правила кавычек

TOON использует кавычки для строк **только когда это необходимо**:
- Пустые строки
- Строки с ведущими или конечными пробелами
- Строки, содержащие разделитель, двоеточие, кавычки или управляющие символы
- Строки, похожие на логические/числовые/нулевые значения

## API и CLI

### JavaScript API
```ts
import { encode } from '@toon-format/toon'

const data = {
  users: [
    { id: 1, name: 'Alice', role: 'admin' },
    { id: 2, name: 'Bob', role: 'user' }
  ]
}

console.log(encode(data))
// users[2]{id,name,role}:
//   1,Alice,admin
//   2,Bob,user
```

### Командная строка
```bash
npx @toon-format/cli input.json -o output.toon
```

## Ограничения и рекомендации

- **Наилучшее применение:** единообразные массивы объектов с одинаковыми ключами и примитивными значениями
- **Не рекомендуется:** неоднородные данные, глубоко вложенные структуры, объекты с различными наборами полей
- **Цель:** формат разработан для **входных данных LLM**, а не для хранения или API
- **Совместимость:** моделирует те же типы данных, что и JSON [RFC8259]

## Связанные темы

[[ai/llm/prompt_engineering/prompt_optimization_techniques.md]] - Техники оптимизации промптов для эффективного использования LLM
[[programming/data_formats/json_format.md]] - JavaScript Object Notation, традиционный формат, с которым сравнивается TOON
[[ai/llm/optimization/llm_token_efficiency.md]] - Эффективность использования токенов в LLM
[[programming/data_formats/yaml_format.md]] - YAML, формат сериализации с которым у TOON схожая структура на основе отступов